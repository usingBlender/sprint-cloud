services:
  web:
    build: .
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro 
      - ./data/data.json:/usr/share/nginx/html/data.json
    command: [nginx-debug, '-g', 'daemon off;']
    ports:
     - "8080:80"
    environment:
     - NGINX_PORT=80